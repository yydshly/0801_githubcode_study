interface:
  display_name: "Three.js Shadow Systems"
  short_description: "Build stable cascades and cached clipmap shadows"
  default_prompt: "Use $threejs-shadow-systems to build a cached clipmap shadow system with texel-snapped committed centers, guarded cross-level blending, bounded refresh budgets, targeted invalidation, and world-texel bias."
