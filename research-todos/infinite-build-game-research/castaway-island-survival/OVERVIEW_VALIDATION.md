# 项目蓝图交付验证

验证日期：2026-08-05  
验证入口：`http://127.0.0.1:4186/overview.html`

## 结论

项目蓝图网页、中文 Markdown 设计文档和当前 V1.2 试玩入口均已落地，满足本轮“先建立整体描述，再用文档固定设计”的目标。页面不把概念图冒充当前游戏截图，系统定义、首个技术切片与路线文档保持一致。

## 浏览器证据

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 桌面首屏 | 通过 | 1280 × 720、DPR 1.5；标题和概念图可见，无横向溢出 |
| 手机首屏 | 通过 | 390 × 844；无横向溢出；主标题、两个入口和阶段信息可读 |
| 概念图 | 通过 | 图片加载完成，原始宽度 1254px |
| 系统浏览器 | 通过 | 共 7 个系统；点击“海洋与物理”后标题和选中态同步更新 |
| 键盘操作 | 通过 | `ArrowRight` 从“海洋与物理”移动到“生态”，焦点与选中态一致 |
| 文档地图 | 通过 | 共 8 张路径卡片，无失效 Markdown 页面跳转 |
| 控制台 | 通过 | 当前页面无 error 级日志 |

手机复核时发现强调语“能够被理解和改变”被拆成“改 / 变”两行，已调整为完整一行并重新验证。

## 工程证据

- `npm.cmd run build` 成功。
- 生产输出同时包含 `dist/index.html` 与 `dist/overview.html`。
- `dist/docs/` 当前包含 9 个 Markdown 文件。
- 本地服务检查：`/`、`/overview.html`、`/docs/README.md` 均返回 HTTP 200。
- CSS 包含 1080px、820px、560px 响应式断点与 `prefers-reduced-motion` 降级规则。

## 已知但不阻塞的问题

现有游戏主脚本生产包约 688.64kB，Vite 给出“大于 500kB”的性能提示。这是原型性能优化项，不影响本轮项目蓝图和文档交付；进入正式 Three.js 技术切片后应拆分模块、资产与场景加载。

## 下一次验收触发条件

下一阶段不是继续扩张整座岛，而是先确认“南部海湾 150 × 150 米技术切片”的引擎与写实基线。完成真实岛屿参考采样、目标设备与技术路线选择后，再建立该切片的画面、碰撞、海水、光照和天气验收表。

## R2 · 陌生岛屿体验修订

修订日期：2026-08-05

### 修订目标

把“陌生环境的恐慌、新鲜感、探索欲、风险和生成”从抽象方向变成可执行规则，并保持真实漂流者的身体、注意力和情绪逻辑。

### 浏览器证据

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 桌面章节 | 通过 | 1280 × 720；4 个情绪阶段、5 步探索语法、生成边界和 4 层风险完整显示，无横向溢出 |
| 桌面模型布局 | 通过 | 探索语法与生成边界两栏等高，均约 531px |
| 手机章节 | 通过 | 390 × 844；情绪、探索模型与风险均切换为单列，无横向溢出 |
| 手机锚点 | 通过 | 双层导航底部约 98px，章节编号定位在约 174px，不再被顶栏遮挡 |
| 既有系统交互 | 通过 | 点击“海洋与物理”正确更新；方向键移动到“生态”，选中态与焦点一致 |
| 控制台 | 通过 | 修订页面无 error 级日志 |

手机首次复核发现章节锚点仍按桌面顶栏高度预留，导致标题第一行被遮挡；已把 560px 以下的 `scroll-margin-top` 调整为 104px，并重新验证。

### 文档证据

- `01_GAME_VISION.md`：增加可信陌生边界、好奇回报与非线性情绪曲线。
- `02_PLAYER_EXPERIENCE.md`：增加最初十分钟现实优先级、探索语法、四层风险与情绪表现原则。
- `03_WORLD_SYSTEMS.md`：增加稳定骨架、动态条件、生成字段、风险计算与条件事件生成。
- `04_WORLD_PRESENTATION.md`：增加休克、陌生声音、谨慎接近、营地安全感和环境突变的表现规则。
- `05_SOUTH_BAY_SLICE.md`：增加三种现实未知诱因、首三分钟行为、固定地图内受控变化和新增玩法验收。
- `07_DEVELOPMENT_ROADMAP.md`：把陌生体验写入阶段门与风险控制。

### 工程证据

- R2 交付时为 8 份 Markdown；R3 新增海难前情后，`dist/docs/*.md` 当前为 9 份。
- `/overview.html` 与 `/` 本地入口均返回 HTTP 200。
- 现有游戏包体积警告保持不变，本次纯内容与页面修订没有增加游戏运行包。

## R3 · 客船海难故事衔接

修订日期：2026-08-05

### 修订目标

让“客船遇到风暴—主角漂流到小岛”的前景形成可信连续故事，同时避免现代大型邮轮会被持续定位、主角长期失联却无人搜救的明显矛盾。

采用偏远群岛地区客货船作为工作设定：风暴连续增强，推进、转向、供电或天线受损，船只在外海礁带撞击；主角穿救生衣被横浪带离，在岛屿背风海湾独自上岸。故事只确认“独自抵达”，不提前确认“唯一幸存”。

### 浏览器证据

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 桌面前情 | 通过 | 1280 × 720；4 项事故事实、5 段时间线、3 个持续问题和救援逻辑完整显示，无横向溢出 |
| 桌面时间线 | 通过 | 5 列各约 235px；标题修订为对称两行“先经历失去 / 再面对一座未知岛屿” |
| 手机前情 | 通过 | 390 × 844；事实、时间线和问题全部变为单列，无横向溢出 |
| 手机锚点 | 通过 | 顶栏底部约 98px，章节编号约 175px，标题未被遮挡 |
| 手机时间线 | 通过 | 5 张卡片宽约 345px，最小高度 285px，内容与状态说明可读 |
| 既有系统交互 | 通过 | 点击“海洋与物理”后内容正确更新；方向键移动到“生态”，焦点与选中态一致 |
| 控制台 | 通过 | 修订页面无 error 级日志 |

桌面首次复核发现标题把“发生”单独留在第二行，已改成语义和视觉都更稳定的两行结构并重新验证。

### 文档证据

- 新增 `docs/08_SHIPWRECK_PROLOGUE.md`，包含地区客货船选择理由、五段事故链、延迟救援逻辑、三个持续悬念、4–6 分钟可玩开场、合理初始状态和禁止的叙事捷径。
- `01_GAME_VISION.md` 明确“独自上岸”不等于“唯一幸存者”。
- `02_PLAYER_EXPERIENCE.md` 把可玩海难前情与岛上观察、倾听、抓取和方向判断连续起来。
- `05_SOUTH_BAY_SLICE.md` 规定伤势、救生衣、手机、漂流物与求救目标必须来自同一事故。
- `07_DEVELOPMENT_ROADMAP.md` 把海难前情加入阶段 0 与生存垂直切片的验收门槛。

### 工程证据

- 生产构建成功，`dist/index.html`、`dist/overview.html` 与 9 个 `dist/docs/*.md` 均生成。
- `/` 与 `/overview.html` 本地入口均返回 HTTP 200。
- 页面当前包含 9 张文档地图卡片。
- 游戏主脚本大小与既有警告保持不变，本次没有增加游戏运行包。

### 尚未锁定但不阻塞本轮的决定

现实参考群岛、具体船型、乘员数量、主角身份和其他幸存者的最终答案仍然开放。它们需要在下一阶段结合现实海事资料、故事主题和引擎表现预算确定；当前文档已明确可修改边界，不使用模糊设定冒充结论。
