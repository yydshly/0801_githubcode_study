# Validation

验证日期：2026-08-03

## 自动检查

| 检查 | 结果 |
| --- | --- |
| `node --check particle-flower-lab/app.js` | 通过 |
| `research-projects.json` 解析 | 通过 |
| `node scripts/build-research-pages.mjs --base=/` | 通过，统一展厅包含 4 个项目 |
| 发布目录 `.pages/projects/particle-flower-lab/` | 已生成 |

## 浏览器验证

| 场景 | 结果 |
| --- | --- |
| 桌面视口 | 14,090 个三角面与约 3,217 个粒子实时运行；完整花冠、花茎和叶片可见 |
| 手机视口 `390 × 844` | 约 2,817 个粒子；无横向溢出，花冠不被顶部裁切 |
| 360° 旋转 | 已观察正面、侧面和背面；背面花萼、枝条及后向花朵可见 |
| 重新绽放 | 主体从根部到花冠完成实体网格显现，然后开始旋转 |
| 粒子解构 / 恢复 | 三角碎片以完整单元移动，粒子同步飞散；恢复后重新闭合 |
| 自动旋转 | `aria-pressed`、按钮文字与实际旋转状态同步 |
| WebGL 回退 | 正常运行时回退提示隐藏 |
| 控制台 | 无 error 或 warning |

## 视觉结论

当前原型证明了无需图片、视频和模型资产，也能用程序化三角网格形成具有正面、侧面和背面的完整花束，再由 GPU 粒子补充边缘飞溅与解构。旧版仅使用点云时花形不清晰、旋转缺少体积；本次验证说明主体必须由实体网格承担，粒子应只作为特效层。
